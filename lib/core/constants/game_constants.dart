/// Глобальные игровые константы — одна точка правды для UI и domain.
const int kPairCount = 8;

/// Задержка перед закрытием двух несовпавших карт (мс).
const Duration kMismatchDelay = Duration(milliseconds: 900);

/// Ширина экрана, с которой сетка переходит на 4 колонки.
const double kWideLayoutBreakpoint = 600;
